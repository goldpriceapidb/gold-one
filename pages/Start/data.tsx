import { QuoteType } from "../../types/QuoteTypes"

export default function Data(): JSX.Element {
	return <></>
}

let quote: QuoteType = {
	verse: `Gold is money, everything else is credit, and once you realize this you'll never be poor again`,
	saidBy: "JP Morgan",
}

export { quote }
